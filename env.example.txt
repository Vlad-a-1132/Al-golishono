# SMTP настройки для отправки email через MasterMail Exchange 2010
# Скопируйте этот файл в .env.local и заполните реальными данными

# SMTP сервер для MasterMail Exchange 2010
SMTP_HOST=smtp.mastermail.ru

# Порт SMTP (25 для Exchange 2010)
SMTP_PORT=25

# Email отправителя (полное имя почтового ящика)
SMTP_USER=zakaz@altamed-c.ru

# Пароль от почты MasterMail
SMTP_PASSWORD=201Rect8*=

# Email получателя (куда будут приходить заявки)
EMAIL_TO=zakaz@altamed-c.ru

